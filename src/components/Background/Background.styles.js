import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  backgroundImage: {
    container: {flex: 1},
    blur: {
      width: '100%',
      flex: 1,
      opacity: 0.8,
      position: 'absolute',
    },
  },
});

export default styles